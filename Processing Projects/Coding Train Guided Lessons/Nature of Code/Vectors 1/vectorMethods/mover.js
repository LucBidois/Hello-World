class Mover {
  constructor() {
    this.position = createVector(width);
    this.velocity = createVector();
    this.acceleration = createVector();
    this.topspeed = 5;
  }

  update() {
  
  }
}
