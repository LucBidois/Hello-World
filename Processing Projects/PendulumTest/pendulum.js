class Pendulum {
  constructor(xpos, ypos, ballAngle) {
    this.stringPos = createVector(xpos, ypos);
    this.pos = createVector();
    this.vel = createVector();
    this.acc = createVector();
    this.stringLength = 100;
    this.angle = ballAngle;
  }
  
  

}
